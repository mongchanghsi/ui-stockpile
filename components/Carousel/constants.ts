/**
 * Card position
 * @returns {Object}
 */
export enum POSITION {
  PREV,
  NEXT,
  NEXT_NEXT,
  CURRENT,
  HIDDEN,
}

/**
 * Cards alignment
 * @returns {Object}
 */
export enum ALIGNMENT {
  HORIZONTAL,
  VERTICAL,
}

/**
 * Cards alignment
 * @returns {Object}
 */
export enum SPREAD {
  NARROW,
  MEDIUM,
  WIDE,
  CUSTOM,
}
